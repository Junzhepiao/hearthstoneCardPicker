<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
            <title></title>
    </head>

    <body>
        <form action="/cards" method="post">
            <input type="number" name="mana" placeholder="mana">
            <input type="number" name="attack" placeholder="attack">
            <input type="number" name="health" placeholder="health">
            <input type="text" name="description" placeholder="name/description">
            <input type="submit">
        </form>
        <h3>Your current List</h3>
        <div>
        <% for (let i = 0; i < deck.length; i++){ %>
        <hr>
            <p><%=deck[i].mana%>,<%=deck[i].attack%>,<%=deck[i].health%>,<%=deck[i].description%></p>
            <a href="/cards/remove/<%=cards[i].id%>"> remove</a>
        <% } %>
        </div>
        <hr>

        <h1>All cards:</h1>
        <div>
        <% for (let i = 0; i < cards.length; i++){ %>
        <hr>
            <p><%=cards[i].mana%>,<%=cards[i].attack%>,<%=cards[i].health%>,<%=cards[i].description%></p>
            <a href="/cards/add/<%=cards[i].id%>"> Add To List</a>
        <% } %>
        </div>
    </body>
</html>